import 'package:flutter/material.dart';

class NewBusSearchDetails extends StatelessWidget {
  const NewBusSearchDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Buss Details"),
      ),
      body: SingleChildScrollView(
        child: Column(
          children: const [],
        ),
      ),
    );
  }
}
